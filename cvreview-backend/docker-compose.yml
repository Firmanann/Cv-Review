version: '3.8'
services:

  #Container Redis
  redis:
    image: redis:alpine
    container_name: cvreview-redis
    restart: always
    command: redis-server --requirepass redismann
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data: